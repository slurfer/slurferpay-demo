services:
  frontend:
    container_name: frontend
    restart: always
    # image: ghcr.io/slurfer/slurferpay-demo:latest
    image: slurferpay-demo:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:80
